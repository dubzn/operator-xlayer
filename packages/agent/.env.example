# X Layer RPC
RPC_URL=https://rpc.xlayer.tech

# Controller wallet (signs intents, pays fees)
CONTROLLER_PRIVATE_KEY=0x...

# Operator backend URL
OPERATOR_URL=http://localhost:3000

# Vault and token addresses
VAULT_ADDRESS=0x...
TOKEN_IN=0x...              # USDT address
TOKEN_OUT=0x...             # WETH address
FEE_TOKEN=0x...             # USDT address (for paying operator fee)

# Swap config
SWAP_AMOUNT=100000000       # 100 USDT (6 decimals)
